import React from "react"

const msg = "Seja bem vindo(a)!! xD"

export default function Primeiro(){
    return (
        <div>
                <h2>Primeiro componente</h2>
                <p>{ msg }</p>
            </div>
    )  
}